<% if @sections.present? %>
  $('#sectionsList').html("<%= escape_javascript(render partial: 'documents/sections/sections_list', locals: { sectionsJSON: @sectionsJSON, currentSectionJSON: @currentSectionJSON, edit: @edit }).html_safe %>");
  $('#sectionsContentEditor').html("<%= escape_javascript(render partial: 'documents/sections/section_content_editor', locals: { sections_size: @sections.size, sections: @sections, section: @currentSection }).html_safe %>");
<% end %>